<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wew.azizchr.guidezprototype">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".MainPage"
            android:screenOrientation="portrait" />
        <activity android:name=".TextBlockWriterActivity" />
        <activity android:name=".ViewPhoto" />
        <activity android:name=".AuthActivity" />
        <activity android:name=".SignUpActivty" />
        <activity android:name=".Homepage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignInActivity" />
        <activity android:name=".ErrorActivity" />
        <activity android:name=".CreateGuideTitle" />
        <activity android:name=".CreateNewGuide" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".EditPhoto" />
        <activity android:name=".AddStepActivity" />
        <activity android:name=".AddDescriptionActivity"></activity>
    </application>

</manifest>